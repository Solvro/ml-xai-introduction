run:
  dir: outputs/${hydra:job.name}/${logging.experiment_name}/${now:%Y-%m-%d_%H:%M}
sweep:
  dir: outputs/${hydra:job.name}/${logging.experiment_name}_sweep/${now:%Y-%m-%d_%H:%M}
  subdir: ${hydra.job.num}
